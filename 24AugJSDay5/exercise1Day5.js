/* function isInArray(array, value) {
  for (index = 0; index < array.length; index++) {
    let element = array[index];

    if (element === value) {
      return true;
    }
  }
  return false;
}

let array = [1, 2, 3, 4, 5, 6, 8, 9];
console.log(isInArray(array, 7));
*/
function isInArray(array, value) {
  for (index = 0; index < array.length; index++) {
    const element = array[i];
    if (element === value) {
      return true;
    }
  }
  return false;
}
console.log(isAnArray([1, 2, 3, 4, 5], 7));
