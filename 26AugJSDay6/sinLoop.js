//for (let y = 0; y < 200; y++) {
//drawSine(y);
//}

function drawLine(spaces, stars) {
  spaces = 0;

  stars = 0;
}

drawLine(2, 3); // "  ***"

//drawLine(10, 1); // "          *"
//x = (Math.sin(y) + 1) * 10
// first, add one so that the value is 0 - 2 instead.
//Then multiply by 10 so that it is from 0 to 10.
//x = (Math.sin(y / 5) + 1) * 10
